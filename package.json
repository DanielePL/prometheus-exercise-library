{
  "name": "prometheus-library",
  "version": "1.0.0",
  "description": "Exercise library for Prometheus fitness app",
  "main": "server/src/index.js",
  "scripts": {
    "start": "node server/src/index.js",
    "server": "server/node_modules/.bin/nodemon server/src/index.js",
    "client": "cd server/client && npm start",
    "dev": "server/node_modules/.bin/concurrently \"npm run server\" \"npm run client\"",
    "install-client": "cd server/client && npm install",
    "build-client": "cd server/client && npm run build",
    "setup": "npm install && npm run install-client"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongodb": "^6.15.0",
    "mongoose": "^7.5.0",
    "multer": "^1.4.5-lts.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.1",
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "private": true
}
